name: build-push-main
on:  
  workflow_run:
    workflows: ['ci']
    types: [completed]
    branches:
    - 'main'

jobs:
  deploy:
    runs-on: ubuntu-22.04
    uses: ./.github/workflows/build-publish-images.yaml
    with:
      ref_name: latest
    secrets: inherit